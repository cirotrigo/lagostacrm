import type { ClientConfig } from '../types';

const config: ClientConfig = {
  id: 'winevix',
  name: 'Wine Vix',
  shortName: 'Wine Vix',
  initial: 'W',
  description: 'Wine Bar & Experiências em Vinhos — Vitória – ES',
  primaryColor: '#722F37',
  hasPublicLanding: true,
  hasDigitalMenu: true,
  metadata: {
    title: 'Wine Vix — Wine Bar & Experiências em Vinhos',
    description: 'Wine bar em Vitória – ES. Rótulos selecionados, harmonizações exclusivas e experiências enogastronômicas.',
  },
};

export default config;
