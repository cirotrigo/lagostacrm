# Exemplo de variáveis de ambiente (NÃO coloque segredos aqui)
# Copie para .env.local e preencha com seus valores.

# --- Next.js / Supabase (App) ---
# URL do projeto no Supabase (Settings → API)
NEXT_PUBLIC_SUPABASE_URL=

# Chave pública do Supabase (Settings → API Keys)
# Novo formato: sb_publishable_... (recomendado)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Fallback legado (se seu projeto ainda usa JWT anon key):
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Chave secreta do Supabase (APENAS server-side).
# Novo formato: sb_secret_... (recomendado)
# Nunca exponha no client e nunca comite.
SUPABASE_SECRET_KEY=
# Fallback legado (se seu projeto ainda usa JWT service_role key):
# SUPABASE_SERVICE_ROLE_KEY=

# --- Dev-only ---
# Habilita rotas internas de teste de IA em desenvolvimento.
ALLOW_AI_TEST_ROUTE=false

# Habilita rotas internas de mock de UI em desenvolvimento.
ALLOW_UI_MOCKS_ROUTE=false

# --- Installer (ops only) ---
# Se INSTALLER_ENABLED nao estiver definido, o instalador fica liberado ate a primeira instalacao.
# Defina como false para desativar o instalador (recomendado apos o setup).
INSTALLER_ENABLED=false

# Token opcional para proteger o instalador (se definido, passa a ser obrigatorio).
INSTALLER_TOKEN=

# --- WPPConnect (WhatsApp Integration) ---
# URL do servidor WPPConnect (ex: https://wpp.seudominio.com)
WPPCONNECT_HOST=

# Token de autenticacao da API WPPConnect (hash bcrypt gerado pelo WPPConnect)
# Este e o token usado para chamar os endpoints da API
WPPCONNECT_TOKEN=

# Secret key para validacao de webhooks (configurada no WPPConnect Server)
# Usada para validar que os webhooks recebidos vem do WPPConnect
WPPCONNECT_SECRET_KEY=

# Nome da sessao (default: lagostacrm)
WPPCONNECT_SESSION_NAME=lagostacrm

# URL base do n8n para webhooks (ex: https://n8n.seudominio.com)
N8N_WEBHOOK_BASE_URL=

# Secret para validar webhooks do n8n (usado pela API de labels sync)
N8N_WEBHOOK_SECRET=

# --- Jobs agendados (Vercel Cron) ---
# Segredo usado para proteger rotas de cron (Authorization: Bearer <CRON_SECRET>)
# Configure este valor em Production na Vercel.
CRON_SECRET=

# --- Chatwoot Integration ---
# URL base do Chatwoot (ex: https://chatwoot.seudominio.com)
# Nota: Credenciais principais ficam na tabela messaging_channel_configs
# Estas sao fallback para desenvolvimento
CHATWOOT_BASE_URL=

# Token de API do Chatwoot (Settings → Account Settings → Access Token)
CHATWOOT_API_TOKEN=

# ID da conta no Chatwoot (numero, ex: 1)
CHATWOOT_ACCOUNT_ID=

# Secret para validar webhooks do Chatwoot (opcional, mas recomendado)
CHATWOOT_WEBHOOK_SECRET=

# --- Instagram Integration (via Chatwoot) ---
# Facebook App ID (do developers.facebook.com)
FACEBOOK_APP_ID=

# Token de verificacao do webhook Instagram/Facebook
# Usado na configuracao do app no Facebook Developer Portal
# Deve ser o mesmo valor configurado no Chatwoot inbox
INSTAGRAM_VERIFY_TOKEN=

# --- Branding (opcional - sobrescreve o que está no banco) ---
# Use quando o banco ainda não está configurado (primeiro deploy)
# Após o bootstrap, o branding vem de organization_settings
NEXT_PUBLIC_BRAND_NAME=
NEXT_PUBLIC_BRAND_SHORT_NAME=
NEXT_PUBLIC_BRAND_INITIAL=
NEXT_PUBLIC_BRAND_DESCRIPTION=
NEXT_PUBLIC_BRAND_PRIMARY_COLOR=
