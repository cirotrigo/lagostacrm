# Exemplo de variáveis de ambiente (NÃO coloque segredos aqui)
# Copie para .env.local e preencha com seus valores.

# --- Next.js / Supabase (App) ---
# URL do projeto no Supabase (Settings → API)
NEXT_PUBLIC_SUPABASE_URL=

# Chave pública (anon) do Supabase (Settings → API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Chave de service role (APENAS server-side). Necessária para alguns scripts e rotas internas dev-only.
# Nunca exponha no client e nunca comite.
SUPABASE_SERVICE_ROLE_KEY=

# --- Dev-only ---
# Habilita rotas internas de teste de IA em desenvolvimento.
ALLOW_AI_TEST_ROUTE=false

# Habilita rotas internas de mock de UI em desenvolvimento.
ALLOW_UI_MOCKS_ROUTE=false

# --- Client Customization ---
# Define qual cliente está ativo para habilitar features específicas.
# Valores: jucaocrm | lagostacrm | default
# Se não definido, usa 'default'.
NEXT_PUBLIC_CLIENT_ID=

# --- Installer (ops only) ---
# Se INSTALLER_ENABLED nao estiver definido, o instalador fica liberado ate a primeira instalacao.
# Defina como false para desativar o instalador (recomendado apos o setup).
INSTALLER_ENABLED=false

# Token opcional para proteger o instalador (se definido, passa a ser obrigatorio).
INSTALLER_TOKEN=

# --- N8N Integration (JucãoCRM/SosPet only) ---
# Webhook URL do N8N para processar importações de produtos em lote
# Necessário apenas se NEXT_PUBLIC_CLIENT_ID=jucaocrm
N8N_WEBHOOK_IMPORT_PRODUCTS=https://seu-n8n.com/webhook/import-products

# Secret compartilhado para autenticar chamadas do N8N
N8N_WEBHOOK_SECRET=

# --- Migration Script (JucãoCRM/SosPet only) ---
# Banco de origem para migração de produtos (Jucao/N8N)
# Usado apenas pelo script: npx tsx scripts/migrate-products-from-jucao.ts
N8N_SUPABASE_URL=
N8N_SUPABASE_KEY=

# Organization ID do SosPet (opcional, tem default)
SOSPET_ORGANIZATION_ID=b859b986-4471-4354-bff4-07313a65c282
